.qodo

node_modules
dist
coverage

.env
.env.local
.env.*.local
.env.docker
tokens.json

# Per-company token store (Docker volume / local data dir)
data/

# Local-only compose override (publishes ports for local runs)
docker-compose.override.yml

start.cmd

# Local capture output (may contain real account balances)
capture-*.txt

